#############################################################################
# Grégoire Boiron <gregoire.boiron@gmail.com>
# Copyright (c) 2018-2021 Grégoire Boiron  All Rights Reserved.
#############################################################################

name: Grammar checks

on:
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:

  jspell-checker:
    name: Run grammar bot
    runs-on: ubuntu-latest
    needs: build
    defaults:
      run:
        shell: bash
    steps:
      - name: Github checkout
        uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: 3.7
          
      - name: install-dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Make script executables
        run: chmod +x scripts/*/*.sh

      - name: Run jspell checker
        run: python ./.github/scripts/validation/jspellCheckerScript.py ${{ secrets.JSPELL_TOKEN }}

      - name: Upload API results
        uses: actions/upload-artifact@v1
        with:
          name: compressed-image
          path: ./.github/scripts/APIResults.txt
