dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install requests=2.22.0
  - pip install ruamel.yaml=0.15.100
  - pip install url
# command to run tests
script:
  - websiteGen.py GITHUB_API_TOKEN
